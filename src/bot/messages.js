export default (bot) => {
    // TODO
    // bot.hears('hi', ctx => ctx.reply('Hey there'));

    // bot.on('text', (context) => {
    //     console.log(context);
    // });

    // bot.on('inline_query', (context) => {
    //     context.reply(`Inline query: ${context}`);
    // });

    // bot.on('callback_query', (context) => {
    //     console.log(context.callbackQuery.data);
    //     context.reply(`Callback query: ${0}`);
    // });
};
